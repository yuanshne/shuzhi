# 这个文件是从 puzzle-server 仓库逐字节复制过来的（vendored），
# 一致性由 puzzle-server/tools/sync_client.sh --check 在 CI 里把关。
# 因此绝不能让 git 的换行转换碰它 —— 一旦检出时被转成 CRLF，
# 校验就会报"漂移"，而代码其实一个字都没变。
src/puzzle-client.js -text

# 构建产物：build.py 统一输出 CRLF。这里必须钉住字节 —— 不钉的话 git 在提交时
# 会把 CRLF 规整成 LF，于是任何非 Windows 平台 clone 下来重建都会"整个文件都变了"
# （内容其实逐字节一样）。CI 里"重建后 git diff 必须为空"那一步靠这个才成立。
index.html -text
